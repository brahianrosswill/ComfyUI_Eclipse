import{app}from'./comfy/index.js';import{createWidgetVisibilityManager,smartResize,notifyVue,onVueModeChange,}from'./eclipse-widget-performance-utils.js';import{injectComboChipCSS,createComboChipWidget as _createComboChipWidget}from'./eclipse-combo-chip.js';const NODE_NAME='Smart Sampler Settings v2 [Eclipse]';const SPECIAL_SEEDS=[-1,-2,-3];const FEATURE_OPTIONS=['allow_overwrite','sampler','scheduler','steps','cfg','guidance','denoise','noise_injection','upscale','image_seed','🎲 img random','⏫ img increment','⏬ img decrement','prompt_seed','🎲 prm random','⏫ prm increment','⏬ prm decrement',];const DEFAULT_FEATURES=['sampler','scheduler','steps','cfg','denoise','image_seed','🎲 img random',];injectComboChipCSS('');const FEATURE_WIDGETS={allow_overwrite:['allow_overwrite'],sampler:['sampler_name'],scheduler:['scheduler'],steps:['steps'],cfg:['cfg'],guidance:['guidance'],denoise:['denoise'],noise_injection:['sigmas_denoise','noise_strength'],upscale:['upscale_steps','upscale_denoise','upscale_value'],image_seed:['image_seed'],prompt_seed:['prompt_seed'],};const IMG_MODE_CHIPS=['🎲 img random','⏫ img increment','⏬ img decrement'];const IMG_MODE_CHIP_TO_VAL={'🎲 img random':-1,'⏫ img increment':-2,'⏬ img decrement':-3,};const IMG_VAL_TO_MODE_CHIP=Object.fromEntries(Object.entries(IMG_MODE_CHIP_TO_VAL).map(([k,v])=>[v,k]));const PRM_MODE_CHIPS=['🎲 prm random','⏫ prm increment','⏬ prm decrement'];const PRM_MODE_CHIP_TO_VAL={'🎲 prm random':-1,'⏫ prm increment':-2,'⏬ prm decrement':-3,};const PRM_VAL_TO_MODE_CHIP=Object.fromEntries(Object.entries(PRM_MODE_CHIP_TO_VAL).map(([k,v])=>[v,k]));const RADIO_GROUPS=[IMG_MODE_CHIPS,PRM_MODE_CHIPS];const ALL_MODE_CHIPS=new Set([...IMG_MODE_CHIPS,...PRM_MODE_CHIPS]);const IMG_SEED_BUTTONS=['_btn_last_image_seed'];const PRM_SEED_BUTTONS=['_btn_last_prompt_seed'];const ALL_CONTROLLED=Object.values(FEATURE_WIDGETS).flat().concat(IMG_SEED_BUTTONS).concat(PRM_SEED_BUTTONS);function createComboChipWidget(node,savedValue,origIdx){const w=_createComboChipWidget({node,options:FEATURE_OPTIONS,savedValue,origIdx,radioGroups:RADIO_GROUPS,radioToggle:true,});w.serializeValue=()=>{const val=Array.isArray(w.value)?w.value:[];return val.filter((f)=>!ALL_MODE_CHIPS.has(f)).join(',');};return w;}
function updateFeatureVisibility(node,vis){if(node.id===-1)return;const raw=vis.getValue('features');const selected=Array.isArray(raw)?raw:[];const selectedSet=new Set(selected);for(const name of ALL_CONTROLLED)vis.setVisible(name,false);for(const feature of selectedSet){const widgets=FEATURE_WIDGETS[feature];if(widgets)for(const name of widgets)vis.setVisible(name,true);}
const imgSeedVisible=selectedSet.has('image_seed')||IMG_MODE_CHIPS.some((c)=>selectedSet.has(c));vis.setVisible('image_seed',imgSeedVisible);for(const name of IMG_SEED_BUTTONS)vis.setVisible(name,imgSeedVisible);const prmSeedVisible=selectedSet.has('prompt_seed')||PRM_MODE_CHIPS.some((c)=>selectedSet.has(c));vis.setVisible('prompt_seed',prmSeedVisible);for(const name of PRM_SEED_BUTTONS)vis.setVisible(name,prmSeedVisible);smartResize(node);}
function generateRandomSeed(){const max=Number.MAX_SAFE_INTEGER;let seed=Math.floor(Math.random()*max);if(SPECIAL_SEEDS.includes(seed))seed=0;return seed;}
function resolveSeed(input,lastSeed){if(!SPECIAL_SEEDS.includes(input))return input;let resolved=null;if(typeof lastSeed==='number'&&!SPECIAL_SEEDS.includes(lastSeed)){if(input===-2)resolved=lastSeed+1;else if(input===-3)resolved=lastSeed-1;}
if(resolved==null||SPECIAL_SEEDS.includes(resolved))
resolved=generateRandomSeed();return resolved;}
function setupSeedChannel(node,seedWidgetName,btnName,lastSeedLabel,statePrefix){const seedWidget=node.widgets?.find((w)=>w.name===seedWidgetName);if(!seedWidget)return;const stateKeys={widget:`_Eclipse_${statePrefix}Widget`,last:`_Eclipse_last${statePrefix}`,button:`_Eclipse_${statePrefix}Button`,cachedInput:`_Eclipse_cached${statePrefix}Input`,cachedResolved:`_Eclipse_cached${statePrefix}Resolved`,};node[stateKeys.widget]=seedWidget;node[stateKeys.last]=undefined;node[stateKeys.cachedInput]=null;node[stateKeys.cachedResolved]=null;const origCb=seedWidget.callback;seedWidget.callback=(v)=>{node[stateKeys.cachedInput]=null;node[stateKeys.cachedResolved]=null;if(origCb)origCb.call(seedWidget,v);};const seedIdx=node.widgets.indexOf(seedWidget);const btnLastSeed=node.addWidget('button',btnName,'',()=>{const last=node[stateKeys.last];if(last!=null){seedWidget.value=last;btnLastSeed.label=lastSeedLabel;btnLastSeed.disabled=true;if(node._Eclipse_chipWidget){const modeChips=statePrefix==='ImageSeed'?IMG_MODE_CHIPS:PRM_MODE_CHIPS;const chips=new Set(node._Eclipse_chipWidget.value);for(const m of modeChips)chips.delete(m);node._Eclipse_updatingChips=true;node._Eclipse_chipWidget.value=[...chips];node._Eclipse_updatingChips=false;const vis=node._Eclipse_vis;if(vis)updateFeatureVisibility(node,vis);}
notifyVue(node);}},{serialize:false});btnLastSeed.label=lastSeedLabel;btnLastSeed.disabled=true;node[stateKeys.button]=btnLastSeed;const btnIdx=node.widgets.indexOf(btnLastSeed);if(btnIdx!==seedIdx+1){node.widgets.splice(btnIdx,1);node.widgets.splice(seedIdx+1,0,btnLastSeed);}
return{seedWidget,stateKeys};}
function syncSeedToChips(node,seedValue,modeChips,valToChip){if(!node._Eclipse_chipWidget)return;const chips=new Set(node._Eclipse_chipWidget.value);for(const m of modeChips)chips.delete(m);if(SPECIAL_SEEDS.includes(seedValue)){const chip=valToChip[seedValue];if(chip)chips.add(chip);}
node._Eclipse_updatingChips=true;node._Eclipse_chipWidget.value=[...chips];node._Eclipse_updatingChips=false;}
function syncChipsToSeed(node,selectedSet,modeChips,chipToVal,seedWidget,parentChip){const activeMode=modeChips.find((m)=>selectedSet.has(m));if(activeMode){if(!selectedSet.has(parentChip)){selectedSet.add(parentChip);}
const modeVal=chipToVal[activeMode];if(seedWidget.value!==modeVal){seedWidget.value=modeVal;seedWidget.callback?.(modeVal);}}else if(selectedSet.has(parentChip)&&SPECIAL_SEEDS.includes(seedWidget.value)){}
if(!selectedSet.has(parentChip)&&!modeChips.some((m)=>selectedSet.has(m))){}}
app.registerExtension({name:'Eclipse.SmartSamplerSettings_v2',async beforeRegisterNodeDef(nodeType,nodeData,_app){if(nodeData.name!==NODE_NAME)return;const origOnNodeCreated=nodeType.prototype.onNodeCreated;nodeType.prototype.onNodeCreated=function(){const ret=origOnNodeCreated?origOnNodeCreated.apply(this,arguments):void 0;const node=this;const vis=createWidgetVisibilityManager(node);node._Eclipse_vis=vis;const autoFeaturesW=node.widgets?.find((w)=>w.name==='features');let featWidget;const origIdx=autoFeaturesW?node.widgets.indexOf(autoFeaturesW):0;let savedValue=DEFAULT_FEATURES.slice();if(autoFeaturesW){const v=autoFeaturesW.value;if(typeof v==='string'&&v.trim()){savedValue=v.split(',').map(s=>s.trim()).filter(Boolean);}else if(Array.isArray(v)&&v.length>0){savedValue=v.slice();}
autoFeaturesW.onRemove?.();node.widgets.splice(origIdx,1);}
featWidget=createComboChipWidget(node,savedValue,origIdx);node._Eclipse_chipWidget=featWidget;for(let i=node.widgets.length-1;i>=0;i--){const wName=(node.widgets[i].name||'').toLowerCase();if(wName==='control_after_generate'){node.widgets.splice(i,1);}}
setupSeedChannel(node,'image_seed','_btn_last_image_seed','♻️ (Use Last Queued Image Seed)','ImageSeed');setupSeedChannel(node,'prompt_seed','_btn_last_prompt_seed','♻️ (Use Last Queued Prompt Seed)','PromptSeed');const origFeatCallback=featWidget.callback;featWidget.callback=function(value){if(node._Eclipse_updatingChips)return;origFeatCallback?.call(this,value);const selectedSet=new Set(Array.isArray(featWidget.value)?featWidget.value:[]);let chipsChanged=false;if(node._Eclipse_ImageSeedWidget){const before=selectedSet.size;syncChipsToSeed(node,selectedSet,IMG_MODE_CHIPS,IMG_MODE_CHIP_TO_VAL,node._Eclipse_ImageSeedWidget,'image_seed');if(selectedSet.size!==before)chipsChanged=true;}
if(node._Eclipse_PromptSeedWidget){const before=selectedSet.size;syncChipsToSeed(node,selectedSet,PRM_MODE_CHIPS,PRM_MODE_CHIP_TO_VAL,node._Eclipse_PromptSeedWidget,'prompt_seed');if(selectedSet.size!==before)chipsChanged=true;}
if(chipsChanged){node._Eclipse_updatingChips=true;featWidget.value=[...selectedSet];node._Eclipse_updatingChips=false;}
updateFeatureVisibility(node,vis);if(autoFeaturesW)autoFeaturesW.value=(Array.isArray(featWidget.value)?featWidget.value:[]).join(',');};requestAnimationFrame(()=>updateFeatureVisibility(node,vis));return ret;};nodeType.prototype._resolveSeed=function(statePrefix){const widget=this[`_Eclipse_${statePrefix}Widget`];if(!widget)return 0;const input=Number(widget.value);const cachedInputKey=`_Eclipse_cached${statePrefix}Input`;const cachedResolvedKey=`_Eclipse_cached${statePrefix}Resolved`;const lastKey=`_Eclipse_last${statePrefix}`;if(this[cachedInputKey]===input&&this[cachedResolvedKey]!=null)
return this[cachedResolvedKey];const resolved=resolveSeed(input,this[lastKey]);this[cachedInputKey]=input;this[cachedResolvedKey]=resolved;return resolved;};const origOnExecuted=nodeType.prototype.onExecuted;nodeType.prototype.onExecuted=function(data){const ret=origOnExecuted?origOnExecuted.apply(this,arguments):void 0;if(data){if(data.image_seed!==undefined)this._Eclipse_lastImageSeed=data.image_seed;if(data.prompt_seed!==undefined)this._Eclipse_lastPromptSeed=data.prompt_seed;}
return ret;};const origOnConfigure=nodeType.prototype.onConfigure;nodeType.prototype.onConfigure=function(data){const ret=origOnConfigure?origOnConfigure.call(this,data):void 0;const node=this;const vis=node._Eclipse_vis||createWidgetVisibilityManager(node);vis.clearCache();requestAnimationFrame(()=>updateFeatureVisibility(node,vis));return ret;};},async setup(){onVueModeChange(()=>{app.graph?.setDirtyCanvas?.(true,true);});const origGraphToPrompt=app.graphToPrompt;app.graphToPrompt=async function(){const result=await origGraphToPrompt.apply(this,arguments);const nodes=app.graph._nodes;for(const node of nodes){if(node.type!==NODE_NAME)continue;if(node.mode===2||node.mode===4)continue;const nodeId=String(node.id);if(!result.output?.[nodeId])continue;const rawFeatures=result.output[nodeId].inputs?.features;if(rawFeatures!=null){if(Array.isArray(rawFeatures)){result.output[nodeId].inputs.features=rawFeatures.filter((f)=>!ALL_MODE_CHIPS.has(f));}else if(typeof rawFeatures==='object'&&'__value__'in rawFeatures&&Array.isArray(rawFeatures.__value__)){rawFeatures.__value__=rawFeatures.__value__.filter((f)=>!ALL_MODE_CHIPS.has(f));}}
const seeds=[{prefix:'ImageSeed',inputKey:'image_seed',label:'♻️ (Use Last Queued Image Seed)'},{prefix:'PromptSeed',inputKey:'prompt_seed',label:'♻️ (Use Last Queued Prompt Seed)'},];for(const{prefix,inputKey,label}of seeds){const widget=node[`_Eclipse_${prefix}Widget`];if(!widget)continue;const resolved=node._resolveSeed(prefix);if(result.output[nodeId].inputs?.[inputKey]!==undefined){const current=result.output[nodeId].inputs[inputKey];if(Number(current)!==Number(resolved))
result.output[nodeId].inputs[inputKey]=resolved;}
if(inputKey==='image_seed'&&result.output[nodeId].inputs?.seed!==undefined){result.output[nodeId].inputs.seed=resolved;}
const lastKey=`_Eclipse_last${prefix}`;if(Number(node[lastKey])!==Number(resolved)){node[lastKey]=resolved;}
node[`_Eclipse_cached${prefix}Input`]=null;node[`_Eclipse_cached${prefix}Resolved`]=null;const btn=node[`_Eclipse_${prefix}Button`];if(btn){const seedVal=widget.value;if(SPECIAL_SEEDS.includes(seedVal)){btn.label=`♻️ ${resolved}`;btn.disabled=false;}else{btn.label=label;btn.disabled=true;}
notifyVue(node);}
if(result.workflow?.nodes){const wfNode=result.workflow.nodes.find((n)=>n.id===node.id);if(wfNode?.widgets_values){const idx=node.widgets.indexOf(widget);if(idx>=0&&wfNode.widgets_values[idx]!==resolved)
wfNode.widgets_values[idx]=resolved;}}}}
return result;};},});