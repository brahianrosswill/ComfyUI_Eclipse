import{app}from'./comfy/index.js';import{patchNodeCSSSize}from'./eclipse-widget-performance-utils.js';const MIN_SLOTS=2;const MAX_SLOTS=64;function scheduleResize(node){setTimeout(()=>{node.setDirtyCanvas(true,false);const computed=node.computeSize();const cur=node.size;const w=Math.max(cur[0],200);const h=Math.max(computed[1]+5,50);if(h>cur[1]||Math.abs(cur[1]-h)>10){node.setSize([w,h]);patchNodeCSSSize(node);}
node.setDirtyCanvas(true,true);},50);}
function getHighestSlotNum(node,prefix){let max=0;if(!node.inputs)return max;const re=new RegExp('^'+prefix+'_(\\d+)$');for(const inp of node.inputs){const m=inp.name?.match(re);if(m){const num=parseInt(m[1],10);if(num>max)max=num;}}
return max;}
function inferSlotType(node,prefix,defaultType){if(defaultType!=='*')return defaultType;if(!node.inputs)return'*';const typed=node.inputs.find(inp=>inp.name?.startsWith(prefix+'_')&&inp.type!=='*');if(typed)return typed.type;const linked=node.inputs.find(inp=>inp.name?.startsWith(prefix+'_')&&inp.link!=null);if(linked){const link=app.graph?.links?.[linked.link]??app.graph?.links?.get?.(linked.link);if(link?.type)return link.type;}
return'*';}
function getLink(id){if(id==null)return null;return app.graph?.links?.[id]??app.graph?.links?.get?.(id)??null;}
function getSourceType(inp){const link=getLink(inp?.link);if(!link)return null;const srcNode=app.graph?.getNodeById(link.origin_id);return srcNode?.outputs?.[link.origin_slot]?.type??link.type??null;}
const NODE_CONFIGS={RvConversion_ConcatMulti:{type:'PIPE',prefix:'pipe',max:MAX_SLOTS},'Concat Pipe Multi [Eclipse]':{type:'PIPE',prefix:'pipe',max:MAX_SLOTS},RvRouter_Any_MultiSwitch:{type:'*',prefix:'any',max:MAX_SLOTS},'Any Multi-Switch [Eclipse]':{type:'*',prefix:'any',max:MAX_SLOTS},RvRouter_Any_MultiSwitch_purge:{type:'*',prefix:'any',max:MAX_SLOTS},'Any Multi-Switch Purge [Eclipse]':{type:'*',prefix:'any',max:MAX_SLOTS},RvConversion_MergeStrings:{type:'STRING',prefix:'string',max:MAX_SLOTS},'Merge Strings [Eclipse]':{type:'STRING',prefix:'string',max:MAX_SLOTS},RvConversion_Join:{type:'*',prefix:'input',max:MAX_SLOTS},'Join [Eclipse]':{type:'*',prefix:'input',max:MAX_SLOTS},RvText_DeDuplicate:{type:'STRING',prefix:'string',max:20},'String DeDuplicate [Eclipse]':{type:'STRING',prefix:'string',max:20},};app.registerExtension({name:'Eclipse.DynamicInputs',async beforeRegisterNodeDef(nodeType,nodeData,appRef){if(!nodeData?.name)return;const name=nodeData.name?.includes('/')?nodeData.name.split('/').pop():nodeData.name;const cfg=NODE_CONFIGS[name];if(!cfg)return;const origOnCreated=nodeType.prototype.onNodeCreated;nodeType.prototype.onNodeCreated=function(){origOnCreated?.apply(this,arguments);const node=this;const isAnyType=cfg.type==='*';const prefix=cfg.prefix;const maxSlots=cfg.max||MAX_SLOTS;const slotName=(num)=>`${prefix}_${num}`;const countWidget=node.widgets?.find(w=>w.name==='inputcount');if(countWidget){countWidget.hidden=true;countWidget.options=countWidget.options||{};countWidget.options.hidden=true;countWidget.computeSize=()=>[0,-4];}
let _localCount=MIN_SLOTS;function getInputCount(){return countWidget?Math.max(MIN_SLOTS,countWidget.value):_localCount;}
function setInputCount(val){val=Math.max(MIN_SLOTS,Math.min(maxSlots,val));if(countWidget)countWidget.value=val;_localCount=val;}
function syncInputs(){node.inputs||(node.inputs=[]);const desired=getInputCount();const slotType=inferSlotType(node,prefix,cfg.type);const existing=new Set();const widgetNames=new Set();for(const inp of node.inputs){if(inp.name?.startsWith(prefix+'_'))existing.add(inp.name);}
for(const w of(node.widgets||[])){if(w.name?.startsWith(prefix+'_'))widgetNames.add(w.name);}
for(let i=1;i<=desired;i++){const name=slotName(i);if(widgetNames.has(name)&&!existing.has(name)){node.addInput(name,slotType,cfg.shape!=null?{shape:cfg.shape}:undefined);existing.add(name);}}
for(let i=1;i<=desired;i++){const name=slotName(i);if(!existing.has(name)){node.addInput(name,slotType,cfg.shape!=null?{shape:cfg.shape}:undefined);existing.add(name);}}
const nums=[];const re=new RegExp('^'+prefix+'_(\\d+)$');for(const name of existing){const m=name.match(re);if(m)nums.push(parseInt(m[1],10));}
nums.sort((a,b)=>b-a);for(const num of nums){if(existing.size<=desired)break;const name=slotName(num);const idx=node.inputs.findIndex(inp=>inp.name===name);if(idx!==-1)node.removeInput(idx);if(node.widgets){const wIdx=node.widgets.findIndex(w=>w.name===name);if(wIdx!==-1)node.widgets.splice(wIdx,1);}
existing.delete(name);}
scheduleResize(node);}
function autoGrow(){const highestNum=getHighestSlotNum(node,prefix);if(highestNum<=0||highestNum>=maxSlots)return;const lastSlot=node.inputs?.find(inp=>inp.name===slotName(highestNum));if(!lastSlot||lastSlot.link==null)return;const newCount=highestNum+1;setInputCount(newCount);syncInputs();}
function autoShrink(){const highestNum=getHighestSlotNum(node,prefix);if(highestNum<=MIN_SLOTS)return;let lastConnected=0;for(let i=1;i<=highestNum;i++){const inp=node.inputs?.find(inp=>inp.name===slotName(i));if(inp?.link!=null)lastConnected=i;}
const keep=Math.max(MIN_SLOTS,lastConnected+1);if(keep<highestNum){setInputCount(keep);syncInputs();}}
function propagateType(connectedType){if(!isAnyType||!connectedType||connectedType==='*')return;for(const inp of node.inputs||[]){if(!inp.name?.startsWith(prefix+'_'))continue;inp.type=connectedType;delete inp.color_on;delete inp.color_off;}
if(node.outputs?.[0]){node.outputs[0].type=connectedType;node.outputs[0].name=connectedType;delete node.outputs[0].color_on;delete node.outputs[0].color_off;}
const color=LGraphCanvas.link_type_colors?.[connectedType];for(const inp of node.inputs||[]){if(!inp.name?.startsWith(prefix+'_')||inp.link==null)continue;const lnk=getLink(inp.link);if(lnk){lnk.type=connectedType;if(color)lnk.color=color;}}
for(const linkId of node.outputs?.[0]?.links||[]){const lnk=getLink(linkId);if(lnk){lnk.type=connectedType;if(color)lnk.color=color;}}
node.setDirtyCanvas(true,true);}
function resetType(){if(!isAnyType)return;const connected=(node.inputs||[]).filter(inp=>inp.name?.startsWith(prefix+'_')&&inp.link!=null);if(connected.length>0){const srcType=getSourceType(connected[0]);if(srcType&&srcType!=='*'){propagateType(srcType);return;}}
for(const inp of node.inputs||[]){if(!inp.name?.startsWith(prefix+'_'))continue;inp.type='*';delete inp.color_on;delete inp.color_off;}
if(node.outputs?.[0]){node.outputs[0].type='*';node.outputs[0].name='';delete node.outputs[0].color_on;delete node.outputs[0].color_off;}
node.setDirtyCanvas(true,true);}
function validateAllConnections(){if(!node.inputs)return;let concreteType=null;for(const inp of node.inputs){if(!inp.name?.startsWith(prefix+'_')||inp.link==null)continue;const srcType=getSourceType(inp);if(srcType&&srcType!=='*'){concreteType=srcType;break;}}
if(!concreteType){resetType();return;}
propagateType(concreteType);}
const origOnConns=node.onConnectionsChange;node.onConnectionsChange=function(direction,slotIdx,connected,linkData){origOnConns?.apply(this,arguments);if(direction!==LiteGraph.INPUT||!this.inputs)return;const inp=this.inputs[slotIdx];if(!inp?.name?.startsWith(prefix+'_'))return;if(connected&&linkData){if(isAnyType){const srcNode=app.graph?.getNodeById(linkData.origin_id);const srcType=srcNode?.outputs?.[linkData.origin_slot]?.type;if(srcType&&srcType!=='*'){const existingType=inferSlotType(node,prefix,'*');if(existingType!=='*'&&existingType!==srcType){setTimeout(()=>node.disconnectInput(slotIdx),0);return;}
propagateType(srcType);}}
autoGrow();}else if(!connected){resetType();requestAnimationFrame(()=>autoShrink());}
this.setDirtyCanvas?.(true,true);};const origOnConfigure=node.onConfigure;node.onConfigure=function(data){origOnConfigure?.apply(this,arguments);if(!countWidget){const highest=getHighestSlotNum(node,prefix);if(highest>_localCount)_localCount=highest;}
setTimeout(()=>{try{if(isAnyType)validateAllConnections();autoShrink();}catch(_){}},150);};setTimeout(()=>{try{syncInputs();if(isAnyType)validateAllConnections();}catch(_){}},80);if(countWidget){let lastVal=countWidget.value;const origCb=countWidget.callback;countWidget.callback=function(){origCb?.apply(this,arguments);if(countWidget.value!==lastVal){lastVal=countWidget.value;syncInputs();}};}};},});