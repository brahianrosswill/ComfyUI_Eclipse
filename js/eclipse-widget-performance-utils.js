if(!document.getElementById('eclipse-tooltip-fix')){const _s=document.createElement('style');_s.id='eclipse-tooltip-fix';_s.textContent='.p-tooltip { pointer-events: none !important; }';document.head.appendChild(_s);}
let _perfFlag='';try{if(typeof localStorage!=='undefined'){_perfFlag=localStorage.getItem('eclipse_perf_log')||'';}}catch{}
if(!_perfFlag&&typeof window!=='undefined'&&window.__eclipse_perf_log){_perfFlag=String(window.__eclipse_perf_log);}
let _perfEnabled=_perfFlag==='1'||_perfFlag==='verbose'||_perfFlag==='true';let _perfVerbose=_perfFlag==='verbose';const _perfCounts=new Map();const _perfCallers=new Map();const _perfFirstSeen=new Map();const _perfDuringLoad=new Map();const _perfLoadStartMs=(typeof performance!=='undefined')?performance.now():Date.now();function _perfCaller(){try{const stack=new Error().stack||'';const lines=stack.split('\n');const start=lines[0]?.startsWith('Error')?4:3;for(let i=start;i<lines.length;i++){const line=lines[i].trim();if(!line)continue;const m=line.match(/\(?(.*?eclipse-[^\/\s:]+\.js)[:\d]*\)?/);if(m){return m[1].split('/').pop();}}
for(let i=start;i<lines.length;i++){const line=lines[i].trim();if(line&&!line.includes('eclipse-widget-performance-utils')){return line.slice(0,120);}}}catch{}
return'<unknown>';}
function _perfTrack(fnName){if(!_perfEnabled)return;_perfCounts.set(fnName,(_perfCounts.get(fnName)||0)+1);if(!_perfFirstSeen.has(fnName)){_perfFirstSeen.set(fnName,((typeof performance!=='undefined')?performance.now():Date.now())-_perfLoadStartMs);}
if(typeof window!=='undefined'&&window.app?.configuringGraph){_perfDuringLoad.set(fnName,(_perfDuringLoad.get(fnName)||0)+1);}
const caller=_perfCaller();let m=_perfCallers.get(fnName);if(!m){m=new Map();_perfCallers.set(fnName,m);}
m.set(caller,(m.get(caller)||0)+1);if(_perfVerbose){console.log(`[eclipse-perf] ${fnName} ← ${caller}`);}}
if(typeof window!=='undefined'&&_perfEnabled){window.eclipsePerfDump=function(){const rows=[];for(const[fn,count]of _perfCounts){const duringLoad=_perfDuringLoad.get(fn)||0;const first=_perfFirstSeen.get(fn)||0;const callers=_perfCallers.get(fn)||new Map();const top=[...callers.entries()].sort((a,b)=>b[1]-a[1]).slice(0,5).map(([c,n])=>`${c}×${n}`).join(', ');rows.push({fn,calls:count,duringLoad,firstSeenMs:Math.round(first),topCallers:top,});}
rows.sort((a,b)=>b.calls-a.calls);console.table(rows);return rows;};window.eclipsePerfReset=function(){_perfCounts.clear();_perfCallers.clear();_perfFirstSeen.clear();_perfDuringLoad.clear();console.log('[eclipse-perf] counters reset');};console.log(`[eclipse-perf] logging ${_perfVerbose ? 'VERBOSE' : 'ON'} (opt-in via localStorage.eclipse_perf_log).  Call window.eclipsePerfDump() for summary.`);}
export function debounce(fn,delay){let timer;return function(...args){clearTimeout(timer);timer=setTimeout(()=>fn(...args),delay);};}
export const canvasDirtyBatcher={markDirty(node,fg=true,bg=false){_perfTrack('canvasDirtyBatcher.markDirty');if(node?.setDirtyCanvas)node.setDirtyCanvas(fg,bg);},};export function notifyVue(node){_perfTrack('notifyVue');const widgets=node.widgets;if(widgets?.length){const last=widgets.pop();widgets.push(last);}}
const _pendingNotify=new Set();let _notifyScheduled=false;export function batchedNotifyVue(node){_perfTrack('batchedNotifyVue');_pendingNotify.add(node);if(!_notifyScheduled){_notifyScheduled=true;queueMicrotask(()=>{_notifyScheduled=false;for(const n of _pendingNotify)notifyVue(n);_pendingNotify.clear();});}}
export function isConfiguringGraph(){try{return!!(typeof window!=='undefined'&&window.app?.configuringGraph);}catch{return false;}}
export function createWidgetVisibilityManager(node){_perfTrack('createWidgetVisibilityManager');const stateCache=new Map();let widgetMap=null;let notifyPending=false;let loadMode=false;if(!node._eclipse_inputPosPatch){node._eclipse_inputPosPatch=true;for(const slot of node.inputs||[]){if(slot.type==='__eclipse_hidden__'){const nd=node.constructor?.nodeData?.input;const def=nd?.required?.[slot.name]||nd?.optional?.[slot.name];slot.type=def?(Array.isArray(def[0])?'COMBO':def[0]):'*';}
delete slot._eclipse_origType;}
if(!isVueMode()){const _origGetInputPos=node.getInputPos;node.getInputPos=function(i){const slot=this.inputs?.[i];if(slot?._eclipse_hidden)return[-1e9,-1e9];return _origGetInputPos.call(this,i);};const _origGetInputOnPos=node.getInputOnPos;const _origGetSlotInPosition=node.getSlotInPosition;node.getInputOnPos=function(e){const result=_origGetInputOnPos.call(this,e);if(result?._eclipse_hidden)return null;return result;};node.getSlotInPosition=function(e,t){const result=_origGetSlotInPosition.call(this,e,t);if(result?.input?._eclipse_hidden)return null;return result;};const _origFindFreeSlot=node.constructor.prototype.findFreeSlotOfType;if(_origFindFreeSlot){node.findFreeSlotOfType=function(type,isOutput,opts){if(isOutput)return _origFindFreeSlot.call(this,type,isOutput,opts);const faked=[];for(const s of this.inputs||[]){if(s._eclipse_hidden&&s.link==null){s.link=-1;faked.push(s);}}
const idx=_origFindFreeSlot.call(this,type,isOutput,opts);for(const s of faked)s.link=null;return idx;};}}}
function findWidget(name){if(!widgetMap||widgetMap.size!==(node.widgets?.length||0)){widgetMap=new Map();for(const w of node.widgets||[])widgetMap.set(w.name,w);}
return widgetMap.get(name);}
let userDriven=false;return{markUserDriven(){userDriven=true;},hideInitially(names){_perfTrack('vis.hideInitially');for(const name of names){const widget=findWidget(name);if(!widget)continue;widget.hidden=true;if(widget.options)widget.options.hidden=true;stateCache.set(name,false);}},setLoadMode(v){loadMode=!!v;},setVisible(name,visible){_perfTrack('vis.setVisible');if(stateCache.get(name)===visible)return;const widget=findWidget(name);if(!widget)return;stateCache.set(name,visible);widget.hidden=!visible;if(widget.options)widget.options.hidden=!visible;if(!isVueMode()){const slot=node.inputs?.find((s)=>s.widget?.name===name);if(slot){if(!visible){if(userDriven&&slot.link!=null){const slotIdx=node.inputs.indexOf(slot);if(slotIdx!==-1)node.disconnectInput(slotIdx);}
slot._eclipse_hidden=true;slot.draw=()=>{};}else{delete slot._eclipse_hidden;delete slot.draw;}}}
if(loadMode||isConfiguringGraph()){userDriven=false;return;}
if(!isVueMode()){userDriven=false;node.setDirtyCanvas?.(true,false);return;}
if(!notifyPending){notifyPending=true;queueMicrotask(()=>{notifyPending=false;userDriven=false;batchedNotifyVue(node);});}},getValue(name){const widget=findWidget(name);return widget?widget.value:null;},clearCache(){stateCache.clear();widgetMap=null;},};}
function _getNodeElement(node){if(node._eclipse_el?.isConnected)return node._eclipse_el;if(null==node.id)return null;node._eclipse_el=document.querySelector(`[data-node-id="${node.id}"]`);return node._eclipse_el;}
function _applyResize(node,minW,minH,padding){if(node.flags?.collapsed)return;const curW=node.size[0];const curH=node.size[1];node.size[1]=0;const computed=node.computeSize();const newH=Math.max(computed[1],minH)+padding;if(newH!==curH){node.setSize?.([curW,newH]);}else{node.size[1]=curH;}
const el=_getNodeElement(node);if(el){el.style.setProperty('--node-height',`${node.size[1]}px`);el.style.setProperty('--node-width',`${curW}px`);}
node.graph?.setDirtyCanvas?.(true,false);}
export function patchNodeCSSSize(node){_perfTrack('patchNodeCSSSize');if(node.flags?.collapsed)return;const el=_getNodeElement(node);if(el){el.style.setProperty('--node-height',`${node.size[1]}px`);el.style.setProperty('--node-width',`${node.size[0]}px`);}}
export function smartResize(node,{minWidth=259,minHeight=100,padding=5}={}){_perfTrack('smartResize');if(node._smartResizePending)return;node._smartResizePending=true;if(!isVueMode()){const runClassic=()=>{if(isConfiguringGraph()){requestAnimationFrame(runClassic);return;}
node._smartResizePending=false;_applyResize(node,minWidth,minHeight,padding);};requestAnimationFrame(runClassic);return;}
const MAX_FRAMES=60;let frames=0;let lastComputedH=-1;let stableCount=0;const tryResize=()=>{if(isConfiguringGraph()){if(++frames>=MAX_FRAMES){node._smartResizePending=false;return;}
requestAnimationFrame(tryResize);return;}
if(!_getNodeElement(node)){if(++frames>=MAX_FRAMES){node._smartResizePending=false;return;}
requestAnimationFrame(tryResize);return;}
const prevSizeH=node.size[1];node.size[1]=0;const computed=node.computeSize()[1];node.size[1]=prevSizeH;if(computed===lastComputedH){stableCount++;}else{stableCount=0;lastComputedH=computed;}
if(stableCount>=1||++frames>=MAX_FRAMES){node._smartResizePending=false;_applyResize(node,minWidth,minHeight,padding);return;}
requestAnimationFrame(tryResize);};requestAnimationFrame(tryResize);}
const _VMC_KEY='__comfy_vueModeCallbacks';const _VMC_LOCK='__comfy_vueModeWatcherInstalled';function _installVueModeWatcher(){if(!window[_VMC_KEY])window[_VMC_KEY]=new Set();if(window[_VMC_LOCK])return;window[_VMC_LOCK]=true;try{let _value=!!LiteGraph.vueNodesMode;Object.defineProperty(LiteGraph,'vueNodesMode',{get(){return _value;},set(v){const prev=_value;_value=!!v;if(prev!==_value){for(const cb of(window[_VMC_KEY]||[])){try{cb(_value,prev);}
catch(e){console.error('vueModeChange callback error',e);}}}},configurable:true,enumerable:true,});}catch{}}
export function isVueMode(){try{return!!LiteGraph.vueNodesMode;}catch{return false;}}
export function onVueModeChange(callback){_installVueModeWatcher();window[_VMC_KEY].add(callback);return()=>window[_VMC_KEY].delete(callback);}
export function removeSocketlessInputs(node){_perfTrack('removeSocketlessInputs');if(isVueMode())return;const nodeData=node.constructor?.nodeData;if(!nodeData?.input)return;const allInputs={...nodeData.input.required,...nodeData.input.optional};const toRemove=[];for(const[name,spec]of Object.entries(allInputs)){if(spec?.[1]?.socketless)toRemove.push(name);}
if(!toRemove.length)return;for(const name of toRemove){const idx=node.inputs?.findIndex(inp=>inp.name===name);if(idx!=null&&idx!==-1)node.removeInput(idx);}}
export default{debounce:debounce,canvasDirtyBatcher:canvasDirtyBatcher,notifyVue:notifyVue,batchedNotifyVue:batchedNotifyVue,createWidgetVisibilityManager:createWidgetVisibilityManager,patchNodeCSSSize:patchNodeCSSSize,smartResize:smartResize,isVueMode:isVueMode,isConfiguringGraph:isConfiguringGraph,onVueModeChange:onVueModeChange,removeSocketlessInputs:removeSocketlessInputs,};